import { DisabledIfDirective } from './disabled-if.directive';

describe('DisabledIfDirective', () => {
  it('should create an instance', () => {
    const directive = new DisabledIfDirective();
    expect(directive).toBeTruthy();
  });
});
