import { FiltroExportPipe } from './filtro-export.pipe';

describe('FiltroExportPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroExportPipe();
    expect(pipe).toBeTruthy();
  });
});
