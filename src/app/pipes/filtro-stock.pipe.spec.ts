import { FiltroStockPipe } from './filtro-stock.pipe';

describe('FiltroStockPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroStockPipe();
    expect(pipe).toBeTruthy();
  });
});
