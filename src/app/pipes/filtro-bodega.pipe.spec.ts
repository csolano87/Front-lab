import { FiltroBodegaPipe } from './filtro-bodega.pipe';

describe('FiltroBodegaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroBodegaPipe();
    expect(pipe).toBeTruthy();
  });
});
