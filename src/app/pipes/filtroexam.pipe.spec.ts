import { FiltroexamPipe } from './filtroexam.pipe';

describe('FiltroexamPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroexamPipe();
    expect(pipe).toBeTruthy();
  });
});
