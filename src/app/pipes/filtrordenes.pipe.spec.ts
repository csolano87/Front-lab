import { FiltrordenesPipe } from './filtrordenes.pipe';

describe('FiltrordenesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrordenesPipe();
    expect(pipe).toBeTruthy();
  });
});
