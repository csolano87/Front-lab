import { FiltroDescargoBodegaPipe } from './filtro-descargo-bodega.pipe';

describe('FiltroDescargoBodegaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroDescargoBodegaPipe();
    expect(pipe).toBeTruthy();
  });
});
